{
  "name": "poker-app",
  "private": true,
  "workspaces": [
    "packages/*",
    "server",
    "client"
  ],
  "scripts": {
    "build": "npm run build -w packages/engine && npm run build -w server && npm run build -w client",
    "test:engine": "npm run test -w packages/engine",
    "dev:server": "npm run dev -w server"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
